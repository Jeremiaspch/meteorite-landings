// @ts-check
/*
import { test, expect } from '@playwright/test';

let app = 'http://localhost:10000/meteorite-landings'
test('hay meteorites listados en meteorite-landings', async ({ page }) => {
  await page.goto('app');

  // Espera a que desaparezca el loader
  await expect(page.getByRole('link')).not.toBeVisible({ timeout: 10000 });

  // Comprueba que hay filas en la tabla
  const rows = page.locator('tbody tr');
  await expect(rows.first()).toBeVisible();
});

*/